# ============================================================
#  EquipLink Backend – Production Profile
# ============================================================

spring:
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://neondb_owner:npg_BTC98mHSoqUp@ep-purple-base-ax3jjy38.c-4.us-east-2.aws.neon.tech/neondb?sslmode=require}
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: false

app:
  jwt:
    secret: ${JWT_SECRET}
  gemini:
    api-key: ${GEMINI_API_KEY}

logging:
  level:
    com.equiplink: INFO
    org.springframework.security: WARN
